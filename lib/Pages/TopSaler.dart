import 'package:flutter/material.dart';

class TopSaler extends StatelessWidget {
  const TopSaler({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Top Saler"),
      ),
    );
  }
}
