class Model {
  final int id;
  final String text;
  Model({required this.id,required this.text});
}

class ModelCity{
  final int id;
  final String city;
  ModelCity({required this.id,required this.city});
}

class AreaModel{
  final int id;
  final String Area;
  AreaModel({required this.id,required this.Area});
}

/*
Center(
child: Card(
child: Column(
mainAxisSize: MainAxisSize.min,
children: <Widget>[
const ListTile(
leading: Icon(Icons.album),
title: Text('The Enchanted Nightingale'),
subtitle: Text('Music by Julie Gable. Lyrics by Sidney Stein.'),
),
Row(
mainAxisAlignment: MainAxisAlignment.end,
children: <Widget>[
TextButton(
child: const Text('BUY TICKETS'),
onPressed: () {*/
/* ... *//*
},
),
const SizedBox(width: 8),
TextButton(
child: const Text('LISTEN'),
onPressed: () {*/
/* ... *//*
},
),
const SizedBox(width: 8),
],
),
],
),
),
);*/
